# Dilworth's Theorem

Dilworth's theorem states that in any finite partially ordered set (poset), the size of the largest antichain is equal to the minimum number of chains needed to cover the poset.

A chain is a subset of the poset where every two elements are comparable, while an antichain is a subset where no two elements are comparable.

minimum number of chains needed to cover everything = maximum size of an antichain