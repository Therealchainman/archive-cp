#include <iostream>
#include <vector>
#include <algorithm>
#include <set>
#include <map>
#include <deque>
#include <queue>
#include <numeric>
#include <stack>
#include <cassert>
using namespace std;
#define endl '\n'
#define LOCAL

using int64 = long long;

#ifdef LOCAL
#include "src/debug.h"
#else
#define debug(...) 42
#endif

void solve() {
    
}

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int T;
    cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}
